# Project Nomad - Agent Instructions

本文件用于约束 AI/自动化工具在本仓库内的产出质量与一致性。

## 语言与风格
- 默认使用中文（变量/类名用英文，注释可中文）。
- 任何机制解释必须引用/对齐 `docs/GDD.md`，禁止“脑补”未确认设定。

## 设计铁律（不可违反）
1. 写实奇幻 + 游牧部族背景，不引入传统魔法师体系扩写。
2. “收服”=灵魂共鸣/调频，不是捕捉或球体封印。
3. 战斗=灵魂投射进入宝可梦体内进行 1v1；遵循非暴力原则设定。
4. 战斗结算=**参数化 Hex-Block** + **30 Tick 同步回合制**：
   - 每回合固定 30 Tick（0-29）。
   - 所有判定必须落在具体 Tick 上。
   - 体积、移动、技能范围必须用 Hex 与 Block 的组合表达。

## 技术约束
- 引擎：Godot 4.x
- 语言：C#
- 2D 模拟 3D：Y-Sort/遮挡必须由参数化格子规则辅助校验。

## 默认落地优先级（与 GDD 一致）
1. Hex-Block 坐标系（Cube Coordinates ↔ 世界坐标）
2. Tick 调度器：`BattleClock`
3. 体积判定：静态占用（1 Hex × 2 Blocks 等）
4. 并行解析：同 Tick 冲突处理
5. 技能模版：参数化 AOE（Hex 集合 + Block 高度）

## 产出要求
- 新增代码前：先在 `docs/` 补充最小必要说明（若涉及机制/规则）。
- 保持“可测试/可验证”：核心算法应提供最小单元测试或可运行的 Debug 场景方案（以仓库现状为准）。

